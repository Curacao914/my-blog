const fs=require('fs');const path=require('path');const root=path.join(__dirname,'../..');const toolsPage=fs.readFileSync(path.join(root,'pages/tools/index.js'),'utf8');const aboutPage=fs.readFileSync(path.join(root,'pages/about/index.js'),'utf8');const systemDesk=fs.readFileSync(path.join(root,'components/SystemDesk.js'),'utf8');const editor=fs.readFileSync(path.join(root,'components/AboutProfileSettings.js'),'utf8');describe('public and desk macOS system v5',()=>{it('keeps tools and about as app windows',()=>{expect(toolsPage).toContain('tools-app-window');expect(aboutPage).toContain('about-app-window');expect(aboutPage).toContain('about-education-card')});it('uses a readable profile editor for owner settings',()=>{expect(systemDesk).toContain('AboutProfileSettings');expect(systemDesk).toContain("section === 'profile'");expect(editor).toContain('profile-editor-grid');expect(editor).not.toContain('JSON.stringify(profile, null, 2)</textarea>')})})
