const fs=require('fs');const path=require('path');const root=path.join(__dirname,'../..');const home=fs.readFileSync(path.join(root,'pages/index.js'),'utf8');const header=fs.readFileSync(path.join(root,'components/law-tech/PublicHeader.js'),'utf8');describe('homepage macOS system v5 repair',()=>{it('keeps the repaired canvas and global dock styling',()=>{expect(home).toContain('home-system-v5-repaired');expect(home).toContain('home-canvas-tools');expect(header).toContain('<style jsx global>');expect(header).toContain('system-dock');expect(header).not.toContain("key: 'search', label: '搜索'")})})
