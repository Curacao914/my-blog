const fs=require('fs');const path=require('path');const root=path.join(__dirname,'../..');const home=fs.readFileSync(path.join(root,'pages/index.js'),'utf8');const header=fs.readFileSync(path.join(root,'components/law-tech/PublicHeader.js'),'utf8');describe('homepage macOS system v4',()=>{it('uses a canvas-style desktop window instead of billboard cards',()=>{expect(home).toContain('home-system-v4');expect(home).toContain('home-canvas-recent');expect(home).toContain('paper-orbit');expect(home).not.toContain('最近翻开的纸页');expect(home).not.toContain('把麻烦事收进抽屉');expect(home).not.toContain('左右拖动切换')});it('keeps system menu and dock as persistent chrome',()=>{expect(header).toContain('system-menu-bar');expect(header).toContain('system-dock');expect(header).toContain('RandomDockLink');expect(header).not.toContain("item.key === 'search'")})})
