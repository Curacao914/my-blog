const fs=require('fs');const path=require('path');const root=path.join(__dirname,'../..');const publicCard=fs.readFileSync(path.join(root,'components/content/PublicContentCard.js'),'utf8');describe('public browse macOS system v5',()=>{it('keeps shared content cards available',()=>{expect(publicCard).toContain('public-content-card')})})
