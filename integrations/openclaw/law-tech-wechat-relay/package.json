{
  "name": "law-tech-wechat-relay",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "outbound": "node src/outbound-poller.js",
    "outbound:once": "node src/outbound-poller.js --once",
    "outbound:probe": "node src/outbound-poller.js --probe",
    "outbound:local": "node src/local-runner.js",
    "outbound:local:once": "node src/local-runner.js --once",
    "outbound:local:probe": "node src/local-runner.js --probe",
    "outbound:install": "bash install-macos-service.sh"
  },
  "peerDependencies": {
    "openclaw": ">=2026.6.10"
  },
  "openclaw": {
    "extensions": [
      "./src/plugin.js"
    ]
  }
}
